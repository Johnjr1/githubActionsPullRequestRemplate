name: Build .NET 8 Project

on:
  pull_request:
    branches:
      - main  # Ändra om du har en annan default branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checka ut koden
        uses: actions/checkout@v4

      - name: Installera .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore beroenden
        run: dotnet restore

      - name: Bygg projektet
        run: dotnet build --no-restore
``
